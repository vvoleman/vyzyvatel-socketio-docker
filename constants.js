const DEBUG = true;

const USER_STATES = {
  MENU: "v menu",
  LOBBY: "v lobby",
  GAME: "ve hře",
};

const ROOM_STATES = {
  LOBBY: "lobby",
  GAME: "game",
  ENDED: "ended",
};

module.exports = {
  DEBUG,
  USER_STATES,
  ROOM_STATES,
};
